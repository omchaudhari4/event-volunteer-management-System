# MySQL Database Configuration
# Edit these properties to match your MySQL Server setup

db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/event_volunteer_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=root
db.password=root

# Fallback to SQLite database file if MySQL server is not running
db.use_sqlite_fallback=true
db.sqlite_url=jdbc:sqlite:event_volunteer.db
